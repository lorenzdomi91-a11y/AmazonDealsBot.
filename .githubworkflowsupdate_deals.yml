name: Update Amazon Deals

on:
  schedule:
    - cron: '0 * * * *' # Gira ogni ora
  workflow_dispatch: 

jobs:
  build:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4 # Versione stabile per Node 24

      - name: Set up Python
        uses: actions/setup-python@v5 # Versione stabile per Node 24
        with:
          python-version: '3.12' # Usiamo una versione di Python più recente

      - name: Install dependencies
        run: |
          pip install requests beautifulsoup4

      - name: Run scraper
        run: python amazon_scraper.py

      - name: Commit and push changes
        run: |
          git config --local user.email "action@github.com"
          git config --local user.name "GitHub Action"
          git add offerte.json
          git commit -m "Update deals [$(date)]" || echo "Nessuna nuova offerta"
          git push
