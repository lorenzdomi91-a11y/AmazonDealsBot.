version: '3.8'

services:
  amazon-bot:
    build: .
    container_name: amazon-offerte-bot
    volumes:
      - ./offerte.json:/app/offerte.json
    ports:
      - "5000:5000"
    environment:
      - PYTHONUNBUFFERED=1
    restart: unless-stopped
    command: python3 server.py
